# Deployment

**Deploy only via Git.** Do not use the Vercel CLI (`vercel` or `vercel --prod`) for this project.

- Push to the `master` branch; Vercel will automatically deploy to **utah-prostate-cancer-run** and update **utahprostatecancerrun.com**.
- One project, one site. All production deploys happen through Git.
