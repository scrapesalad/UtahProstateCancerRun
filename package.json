{
  "name": "utah-prostate-cancer-run",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build step required for static site'",
    "dev": "live-server"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
